package com.cch.pokemon.repository;

import com.cch.pokemon.entity.RegionDresseur;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegionDresseurRepository extends JpaRepository<RegionDresseur, Long> {
}
